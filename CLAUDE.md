# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status: pre-development

This repository is in pre-development. The only tracked content is the `LICENSE` (MIT, Copyright 2026 TeaHouse) and the initial commit. There is no source code, build system, dependency manifest, test suite, or established architecture to document yet.

Do not fabricate commands, structure, or conventions for this project — none exist. If a future task references a file, tool, or workflow that is not present in the working tree, verify against the current state before acting and treat any stale reference as outdated.

## When real code lands

Once source files, a module manifest (e.g. `go.mod`), a build script, or tests are added, regenerate this file with the standard `/init` flow so it accurately reflects:
- the build / run / test commands
- the high-level architecture
- any project-specific conventions
